{
  "query": {
    "kind": "HogQLQuery",
    "query": "SELECT JSONExtractString(properties, '$pathname') as url, count() as views, uniqExact(distinct_id) as uniq FROM events WHERE event = '$pageview' AND timestamp > now() - toIntervalHour(24) GROUP BY url ORDER BY views DESC"
  }
}