{
  "query": {
    "kind": "HogQLQuery",
    "query": "SELECT replaceAll(toString(properties['$current_url']), 'https://vaos.sh', '') as url, count() as views, uniqExact(distinct_id) as uniq FROM events WHERE event = '$pageview' AND timestamp > now() - toIntervalDay(1) GROUP BY url ORDER BY views DESC LIMIT 20"
  }
}